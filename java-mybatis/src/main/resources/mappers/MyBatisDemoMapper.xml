<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatisDemo">
    <update id="createTable">
        CREATE TABLE mybatis_demo (
            id BIGINT AUTO_INCREMENT PRIMARY KEY,
            name VARCHAR(100) NOT NULL,
            amount DECIMAL(12, 2) NOT NULL
        )
    </update>

    <insert id="insertRow" parameterType="map">
        INSERT INTO mybatis_demo (name, amount)
        VALUES (#{name}, #{amount})
    </insert>

    <update id="updateAmount" parameterType="map">
        UPDATE mybatis_demo
        SET amount = amount + #{delta}
        WHERE name = #{name}
    </update>

    <select id="selectRows" resultType="map">
        SELECT
            id AS "id",
            name AS "name",
            amount AS "amount"
        FROM mybatis_demo
        ORDER BY id
    </select>
</mapper>
